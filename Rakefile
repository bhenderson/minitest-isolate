# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'minitest-isolate' do
  developer('bhenderson', 'henderson.bj@gmail.com')

  extra_deps << ['isolate']
  extra_deps << ['minitest']
end

# vim: syntax=ruby
